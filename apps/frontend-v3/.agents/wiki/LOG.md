# Wiki Log

- 2025-04-20: Created anti-patterns.md — type casts, auth redirect, error handling, props, route size
- 2025-04-20: Created auth-architecture.md — discriminated union, useSession, register flow, profile model, 401
- 2025-04-20: Created api-conventions.md — response format, api.ts scope, query patterns, middleware
- 2025-04-21: Created tanstack-router.md — file-based routing, validateSearch, auth guards
- 2025-04-21: Created tanstack-query.md — QueryClient setup, query/mutation patterns
- 2025-04-21: Merged design-tokens, icon-patterns, provider-pattern into component-patterns skill
- 2025-04-21: Moved api-integration architecture content to wiki (api-conventions, tanstack-query)
- 2025-04-21: Created state-patterns.md — useReducer vs useState, useMutation for all writes, mutation side effects
- 2025-04-21: Updated auth-architecture.md — auth store owns all tokens, switchProfile action, no external helpers
- 2025-04-21: Updated anti-patterns.md — API calls outside useMutation, token manipulation outside store
- 2025-04-21: Rewrote api-conventions.md — full standard: response formats, null handling, data flow, feature folder, mutations, store
- 2025-04-21: Created fsrs-migration.md — FSRS v6 replaces SM-2, state model, frontend impact
- 2025-04-21: Created srs-lessons.md — research first, fix root cause, RFC for cross-cutting changes
- 2025-04-21: Created practice-ux.md — transcript free in practice, listening layout decisions
- 2026-04-22: Updated anti-patterns.md — Biome formatter JSX inline rule (lineWidth 110, bunx biome check after every edit)
- 2026-04-22: Updated anti-patterns.md — useMediaCaption no-suppress, useReducer Map/Set generics, exam device-check phase mandatory
- 2026-04-22: Created exam-color-system.md — exam room dùng primary cho interactive, skill color chỉ cho identifier
- 2026-07-17: Created grading-fallback-bug.md — $systemPrompt never passed to callStructuredLlm, LLM had no instructions → always fallback 5.0
- 2026-05-14: Audit toàn bộ thay đổi branch hiện tại. Backend: tạo AdminSpeakingDrillService, AdminSpeakingTaskService, AdminWritingService để fix thin-controller violation. Admin frontend: route pages > 100 lines là tech debt tồn đọng, cần split components dần.
- 2026-04-24: Created auth-google.md — Google Sign-In via GSI renderButton, BE verifies JWKS with openssl_verify (no lib dep), needs_onboarding flow → Step2 completes initial profile
- 2026-04-25: Updated anti-patterns.md — SkillChip shared component rule (no inline pill, no dot inside chip); created `components/SkillChip.tsx`
- 2026-04-25: Created gamification-ui.md — Duolingo-ish chip/popup/dropdown/notif patterns; coin-gain store + Header animation rules (single-fire via useRef, fire after popup close)
- 2026-04-25: Created duo-progress-bar.md — track + fill + highlight stripe layering; reusable DuoProgressBar component; tone tokens (primary/coin/warning/info/streak each with `*-light` for highlight)
- 2026-04-25: Updated tanstack-query.md + anti-patterns.md — invalidateQueries matches by prefix not equality; child key never invalidates parent; recurring "must F5" bug → always invalidate the shortest shared prefix
- 2026-04-26: Created icon-criteria-icons8.md — tiêu chí chuẩn lấy icon từ icons8.com: style Color, bo tròn, stroke-width 2-3, currentColor cho mono, palette mapping cho static, clean SVG checklist, naming convention
- 2026-04-26: Audited 46 SVG icons hiện có vs Duolingo style. Tạo 12 icon `*-v2.svg` cho các icon không đạt tiêu chí (mono icon hardcode hex thay vì currentColor: back, close, trash, weights, share, invite, timer-small/xs, graduation, house; coin với doodle viewBox 430). Giữ nguyên icon cũ, register parallel keys (`back-v2`, `coin-v2`, `coin-md-v2`...) trong Icon.tsx
- 2026-04-26: REWROTE icon-criteria-icons8.md sau khi user feedback: Duo style là MULTI-TONE với identity color (yellow house + red roof, blue weights, green target, purple guidebook), KHÔNG phải mono đen. Phân loại lại: chỉ utility icons (back/close/share/invite/check/search/more) được phép mono `currentColor`; các icon vật thể PHẢI là static multi-tone với identity color theo palette skill. Vẽ lại 8 icon `-v2` (house/weights/graduation/trash/timer-small/timer-xs/coin-small/coin-medium) thành multi-tone, di chuyển từ `icons` map sang `staticIcons` map. 4 icon utility (back/close/share/invite) giữ mono currentColor là đúng.
- 2026-04-26: Found Iconify free SVG API matches Duo style: **Fluent Emoji Flat** (MS, MIT, 3145 icons) là source chính (multi-tone thật yellow+red+blue). icons8.com không có public free SVG API. Streamline Plump-Color trông chunky nhưng monochromatic blue, KHÔNG match Duo. Replaced 8 self-drawn `-v2` icons bằng SVG thật từ Fluent Emoji Flat (house, flexed-biceps→weights, graduation-cap, wastebasket→trash, timer-clock, coin). Updated wiki với pipeline Iconify search + curl + size fix (1em → pixel).
- 2026-05-07: Created practice-layout-pattern.md — unified layout pattern cho tất cả trang luyện tập. Shared LevelFilters component (A1–C1 toggle). Tất cả dùng ExerciseCard. Ngữ pháp: group by category + LevelFilters. Từ vựng: TopicGrid chuyển sang ExerciseCard + LevelFilters. Nghe/Đọc/Viết: xoá PartFilters, giữ group by part heading. Xoá GrammarFilters (duplicate). Grammar detail: flat scroll, không tab/sticky bar.
- 2026-04-26: User reject all `-v2` (Fluent Emoji không phù hợp Duo). Adopt FE-v2 criteria: **Plasticine PNG 256px from icons8.com** (URL `https://img.icons8.com/plasticine/256/{slug}.png`). Audit FE-v2 thực tế dùng Plasticine (không phải 3D Fluency như doc nói — doc lý thuyết, code dùng Plasticine: header byte verify 256px raster). Tải 4 skill icons (headphones, book, microphone, pencil) vào `public/icons/`. Created `<SkillIcon>` component render `<img src="/icons/{name}.png">`. Added `pngIcon` field vào `lib/skills.ts`. Rewrote wiki theo tiêu chí FE-v2.
- 2026-04-29: Created vocab-practice.md — unified `usePracticeSession` thay 3 hook riêng (flashcard/typing/exercise). 5-phase state machine: prompt→checking→reveal. Wrong-auto-reveal. fill_blank generate FE-only từ word.example (regex mask). FlipCard CSS 3D animation chỉ cho flashcard mode, các mode khác static layout. 6 icons mới (flashcard, keyboard, headphones, swap, fill-blank, shuffle).
- 2026-04-29: Created web-speech.md — Microsoft David clip ~500-800ms đầu utterance trên Chromium (cold start). Fix: voice priority list ưu tiên Microsoft Aria/Jenny/Guy Online (Azure Neural TTS), avoid David. Tried và bỏ: setTimeout, queueMicrotask, primer utterance, prefix dots — đều không fix được nguồn gốc. Voice list match là solution duy nhất.
- 2026-05-01: Created speaking-practice.md — Conversation AI (roleplay + LLM grading) + Shadowing (TTS + SpeechRecognition + DP word alignment). Removed old drill/VSTEP speaking. Workers AI thay OpenAI (quota hết). Chrome TTS cold start fix: "..." filler prefix. SpeechRecognition auto-restart + stoppedRef prevent double submit. compareWords() moved to lib/utils.ts. 8 conversation scenarios + 6 shadowing lessons seeded.
- 2026-05-05: Updated speaking-practice.md — IPA generation (backend LLM per conversation turn + opening line), shadowing progress persisted to DB (was localStorage), TTS auto-play + 500ms delay fix for shadowing, phiên âm toggle button on conversation turns, level/status filter pills on speaking page, data expanded to 21 scenarios (A1-C1) + 10 shadowing lessons (A1-C1), ExerciseCard tag prop for "Đã luyện".
- 2026-05-06: Updated web-speech.md — TTS listening practice: Web Speech fallback khi không có audio file. Dual-mode word highlight (timer fallback cho Google voices không fire onboundary + exact boundary cho Microsoft voices). Chrome 15s timeout workaround (pause/resume interval). Dialogue parsing (Speaker: prefix) + 2 giọng cho Part 2. skipCancel option cho sequential utterances. Fixture data sửa: title tiếng Việt→English, audio_url null cho TTS, speaker labels thêm vào transcript.
- 2026-05-07: Updated web-speech.md — `skipCancel` option was dead code; `warmupTTS()` referenced `opts` (parameter of `speak()`, not `warmupTTS`) causing "opts is not defined" crash. Removed dead option, `warmupTTS` now calls `synth.cancel()` unconditionally.
- 2026-05-18: Updated speaking-practice.md — Mock data → API migration. Deleted `mock-shadowing.ts` and `mock-conversation.ts`. Added `shadowingLessonsQuery` + `shadowingLessonDetailQuery` to `queries.ts`. Backend added shadowing fields (audio_url, ipa, word_count, audio_start, audio_end) + seed migration. Admin: new Speaking Scenarios CRUD, enhanced Speaking Drills form with timing fields, deleted Speaking Tasks (VSTEP format not used).
- 2026-05-25: Created duolingo-style-reference.md — extended Duolingo-inspired design reference with color/type/spacing tokens, component rules, agent prompt guide, and CSS/Tailwind v4 quick-start snippets.
- 2026-05-25: Created duolingo-design-tokens.json — agent-readable audit tokens mapping Duolingo reference values to current VSTEP semantic `@theme` tokens, including project variants for typography, spacing, radius, and component rules.
- 2026-05-25: Updated auth-architecture.md — learner avatar is owned by each profile; active-profile avatar updates no longer affect sibling profiles under one account.
- 2026-05-25: Updated auth-architecture.md + anti-patterns.md — migrated avatar files remain until no profile references them; backend Docker tests must explicitly target `vstep_test`.
- 2026-05-25: Updated speaking-practice.md — registered backend `resources/ai` prompt view location after conversation turn/review/pronunciation endpoints failed with missing `ai.*` Blade views.
- 2026-05-26: Updated anti-patterns.md — inline SVG icon trong section component (LandingSkills `CheckMark`, LandingFeatures `Bullet` cùng path checkmark); rule: grep `Icon.tsx` trước, dùng `<Icon name="check">` thay vì viết lại path SVG dù chỉ vài dòng.
- 2026-05-26: Restored backend `View::addLocation(resource_path())` after provider changes regressed `ai.conversation.pronunciation` resolution; added endpoint regression test for shadowing pronunciation review.
- 2026-05-26: Fixed AI conversation turn prompt → normalizer mismatch. Root cause: `turn.blade.php` prompt asked LLM for flat JSON keys (`grammar_ok`, `vocab_check`, etc.) but `ConversationTurnNormalizer` expected `$data['feedback']` nested object. Added explicit RESPONSE FORMAT block with nested `feedback` key + `extractJson()` to strip markdown fences in `callWithRetry`. Same fix applied to `review.blade.php` and `pronunciation.blade.php`.
- 2026-05-26: Fixed AI conversation session conflict (422) when exiting mid-conversation. Backend: `startSession` now auto-ends existing active sessions instead of throwing `ValidationException`. Frontend: `ConversationInProgress` cleanup on unmount only cleans local resources — no `endConversation` API call (prevents StrictMode double-fire from ending sessions prematurely). Tab close handled via `beforeunload` + `sendBeacon`.
- 2026-05-26: Discovered React StrictMode anti-pattern: `useEffect` cleanup with `endConversation()` API call fires twice in dev (StrictMode mount→unmount→mount cycle), ending the session immediately after creation. Fix: keep cleanup side-effect-free (only `stopSpeaking()`), rely on backend auto-end for stale sessions.
- 2026-05-26: Integrated `phonemize` (MIT, pure JS, 125K word dictionary) for client-side IPA generation. Pattern: `getIpa(text, adminIpa)` — admin override takes priority, falls back to `toIPA(text)`. Uses **async dynamic import** (`import("phonemize")`) to prevent module load failure from crashing conversation components. Created `lib/phonemize.ts` with `getIpa()` async + `getIpaSync()` for admin override instant display.
- 2026-05-26: Added `--color-destructive-dark: color-mix(in oklch, #ea4335 70%, black)` to `styles.css` following existing pattern (`--color-skill-speaking-dark`). Replaced 3 hardcoded hex `#b5322a` instances in `ConversationInProgress.tsx` with `var(--color-destructive-dark)`.
- 2026-05-26: Improved AI conversation error handling — `submitTurn` + `review` error messages now show backend status code and message (`Lỗi server (XXX)` instead of generic "Không thể gửi"). Added 409 handling (session already ended → auto-complete state).

- 2026-05-26: Fixed dashboard + practice SkillIcon sizes after trimming PNGs. 6 icon files (listening, reading, writing, speaking, vocabulary, grammar) were 1024x1024 with massive transparent padding (~1.4MB each). Trimmed to content bounds (163-256px, 50-87KB) using sharp, then removed sharp from deps. Adjusted SkillIcon sizes across dashboard (xs->sm for legend rows, y=198->210 for ScoreTrend dates) and practice (lg->md for card heroes) to compensate for icons now filling their canvas. Removed disabled Xem them placeholder from Sidebar. SpiderCard layout fix: flex flex-col + flex-1 on chart to push legend to card bottom. No rule violations - all changes follow existing SkillIcon pattern, no hardcoded values, no inline helpers.
